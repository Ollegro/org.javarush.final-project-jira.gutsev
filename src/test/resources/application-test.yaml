spring.cache.type: none
spring:
  init:
    mode: always
  datasource:
    url: ${spring.datasource_db_test.url}
    username: ${spring.datasourse_db_test.username}
    password: ${spring.datasourse_db_test.password}