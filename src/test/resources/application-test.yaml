spring.cache.type: none
spring:
  init:
    mode: always
  datasource:
    url: ${SPRING_DATASOURCE_DB_TEST_URL}
    username: ${SPRING_DATASOURCE_DB_TEST_USERNAME}
    password: ${SPRING_DATASOURCE_DB_TEST_PASSWORD}